{
  "name": "Tres Ríos",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev-https": "set NODE_TLS_REJECT_UNAUTHORIZED=0 && local-ssl-proxy --source 3010 --target 3000 --cert localhost.pem --key localhost-key.pem & nuxt dev",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue,.ts --fix .",
    "format": "prettier --write --list-different './plugins/**' './storyblok/**' './components/**' './composables/**' './pages/**' './server/**' './layouts/**' app.vue",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "nuxt": "^3.6.5",
    "sass": "^1.66.1",
    "sass-loader": "^13.3.2"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.8.0",
    "@storyblok/nuxt": "^5.6.5",
    "local-ssl-proxy": "^2.0.5"
  }
}
